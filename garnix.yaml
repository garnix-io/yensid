servers:
  - configuration: deployment
    deployment:
      type: on-branch
      branch: readme
  - configuration: deployment-builder
    deployment:
      type: on-branch
      branch: readme

builds:
  exclude:
  - nixosConfigurations.ca
  - nixosConfigurations.proxy
  - nixosConfigurations.client
  - nixosConfigurations.builder1
  - nixosConfigurations.builder2
